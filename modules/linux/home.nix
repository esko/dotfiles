{ config, lib, pkgs, username, homeDirectory, stateVersion, hostName, ... }:

{
  # Linux-only packages and desktop integration are added after the foundation
  # has been reviewed. This module is the stable host boundary for both
  # Crostini and the future Bruschetta installation.
}
