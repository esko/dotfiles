# Public SSH keys

This directory contains public per-deployment SSH keys generated by
`nix run .#bootstrap-ssh -- <deployment>`. Public keys are safe to commit; their
matching private keys exist only inside SOPS-encrypted host files.
