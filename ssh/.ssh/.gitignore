id_*
!id_*.pub
!id_*.age
!id_*.gpg

# Home Manager links the active host's public key here at activation time.
/id_ed25519.pub
/authorized_keys

# Home Manager writes host-specific SSH snippets at activation time.
config.d/
