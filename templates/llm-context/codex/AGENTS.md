# Codex shared context

When the private context checkout is available, read the reviewed durable
guidance at `$LLM_CONTEXT_DIR/shared/CONTEXT.md` and the relevant project
notes. Do not load auth files, transcripts, caches, or local state databases.

This template is intentionally generic. Keep personal and project-specific
instructions in the private checkout, not in public dotfiles.
