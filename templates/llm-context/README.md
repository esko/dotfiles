# LLM context templates

These files are sanitized starting points for a separate private context
checkout. They contain no credentials, transcripts, or machine state.

Copy reviewed files to the private repository under `tools/`; do not link this
public template tree directly into an agent client's configuration.
