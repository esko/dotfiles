# Cursor hooks

This empty hook map is a sanitized placeholder. Add project-specific hooks
only in the private context checkout after confirming the installed Cursor
schema. Hooks must not print, read, or transmit credentials or transcripts.
