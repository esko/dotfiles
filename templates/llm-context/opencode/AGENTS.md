# OpenCode shared context

Use reviewed project notes from `$LLM_CONTEXT_DIR` when available. Keep
provider credentials, transcripts, caches, and runtime state outside the
private context repository.
