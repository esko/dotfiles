# Claude shared context

Use the reviewed durable guidance in `$LLM_CONTEXT_DIR/shared/CONTEXT.md`
when that private checkout is present. Keep credentials, session history,
plugin state, and machine-local paths out of shared context.
